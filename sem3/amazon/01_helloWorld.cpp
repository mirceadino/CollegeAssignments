#include "library.h"

( DATA DECLARATIONS )
DECLARE(init)


( WORD AND CONST DECLARATIONS )
CONST(N, 0)
CONST(U, 1)
CONST(D, 2)
CONST(L, 3)
CONST(R, 4)
CONST(END, -1)

  
( CODE )
IF(init) THEN
ELSE
  ASSIGN(init, 1)
  PUSH_ADDR(END)
  PUSH_ADDR(D)
  PUSH_ADDR(R)
  PUSH_ADDR(U)
  PUSH_ADDR(R)
  PUSH_ADDR(D)
FI

( END )
quit
